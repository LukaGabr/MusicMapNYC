import Foundation

// TEMPLATE FILE — this one is safe to commit.
//
// To use: copy this file, rename the copy to "Secrets.swift" (remove ".example"),
// paste your real Discogs token in below, and add it to your Xcode target.
// Secrets.swift itself is in .gitignore, so your real token never gets pushed
// to GitHub — only this template does.
enum Secrets {
    static let discogsToken = "YOUR_DISCOGS_TOKEN_HERE"
}
